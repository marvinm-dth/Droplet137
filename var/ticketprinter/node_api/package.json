{
  "name": "ticketprinter-api",
  "version": "0.1.0",
  "private": true,
  "description": "Bridge REST API that proxies to the Flask server for ticketprinter data (no DB copy on the Node side).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
